public class tblEmployeeMakeDummy {
    public static void main(String[] args) {

        /*
        for (int i=0; i<20; ++i) {
            System.out.printf("%s (employeeseq.nextval, '%s%s%s%s%s%s%s%s%s%s', '%s%s%s%s%s%s%s%s%s%s', '%s', '서울시 %s구', '010-%s%s%s%s-%s%s%s%s', %d);\n", tblemployee,

                    id1[random.nextInt(id1.length)],
                    id1[random.nextInt(id1.length)],
                    id1[random.nextInt(id1.length)],
                    id1[random.nextInt(id1.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],

                    id1[random.nextInt(id1.length)],
                    id1[random.nextInt(id1.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id3[random.nextInt(id3.length)],
                    id3[random.nextInt(id3.length)],

                    "이사",

                    city[random.nextInt(city.length)],

                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],
                    id2[random.nextInt(id2.length)],

                    random.nextInt(10)+45

            );
        }

         */
    }
}
